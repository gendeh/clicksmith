{
  "name": "clicksmith",
  "version": "1.0.0",
  "description": "Windows-first, overlay-based, human-in-the-loop input automation application",
  "private": true,
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:backend\" \"npm run dev:image-service\"",
    "dev:client": "cd client && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:image-service": "cd image-service && python -m flask run --port 5001",
    "build": "npm run build:client && npm run build:backend",
    "build:client": "cd client && npm run build",
    "build:backend": "cd backend && npm run build",
    "test": "npm run test:client && npm run test:backend && npm run test:image-service",
    "test:client": "cd client && npm test",
    "test:backend": "cd backend && npm test",
    "test:image-service": "cd image-service && pytest",
    "lint": "npm run lint:client && npm run lint:backend",
    "lint:client": "cd client && npm run lint",
    "lint:backend": "cd backend && npm run lint",
    "package": "cd client && npm run package",
    "installer": "cd installer && makensis clicksmith.nsi"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/clicksmith/clicksmith.git"
  },
  "author": "Clicksmith Team",
  "license": "PROPRIETARY"
}
