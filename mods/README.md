# Clicksmith Mod Registry

This folder contains the adapter registry consumed by the Clicksmith client.

- `registry.json` lists supported mod adapters and their install/protocol metadata.
- Edit this file to add new adapters or to override defaults for development.

See `docs/mods/manifest.md` for the manifest format and `docs/mods/protocol.md` for the
Clicksmith Mod Protocol.
