# Security & Privacy

- Clicksmith avoids memory injection and code modification.
- Input hooks operate at the OS level only when recording or playback is active.
- Screenshot patches are stored locally by default.
- Cloud sync is opt-in and uses Firebase encryption at rest.
- Telemetry is opt-in and minimal.
